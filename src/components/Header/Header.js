import Heading from "./Heading/Heading";
import Hero from "./Hero/Hero";

const Header = (props) => {
  return (
    <div>
      <Heading/>
      <Hero/>
    </div>
  )
}

export default Header
